# Polling interval in seconds (default: 60)
interval: 60

# Fetch full post content (text + attachments) via Innertube (default: false)
# fetchPost: true

webhooks:
  discord:
    - url: https://discord.com/api/webhooks/...
      msg: ""
